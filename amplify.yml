version: 1
applications:
  - appRoot: app
    frontend:
      phases:
        preBuild:
          commands:
            - nvm install 20
            - nvm use 20
            - node -v
            - npm i -g npm@latest
            - cd .. && npm ci && npx ampx generate outputs --branch $AWS_BRANCH --app-id $AWS_APP_ID && cd app
            - npm ci --prefer-offline
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
          - node_modules/**/*

